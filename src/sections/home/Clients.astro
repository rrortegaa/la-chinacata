---
import { Image, imageConfig } from "astro:assets";
import luis from "../../assets/home/clients/luis-n.jpeg";

const reviews = [
  {
    name: "Luis N.",
    review:
      "This family owned business has come a long way and they  have never sacrificed quality even when times got tough. Food and service had always been the forefront, and they take a lot of pride in the food they make. If you haven't had their aguas frescas, they were always the best on a warm day.",
    image: luis,
  },
  // {
  //   name: "Sara & John",
  //   review:
  //     "We hired La Chinacata for our corporate event and we were very pleased with the service. The food was delicious and the staff was very professional.",
  //   image: cheers,
  // },
];
---

<section id="reviews" class:list={["section"]}>
  <header
    class:list={[
      "flex flex-col gap-7",
      "mx-auto",
      "max-w-2xl",
      "[&_p]:text-lg",
      "font-light text-center text-green-leaf-800",
    ]}
  >
    <h2
      class="font-serif text-heading-sm md:text-heading-lg text-french-rose-400"
    >
      Celebrating with homemade flavor & happy clients
    </h2>

    <p>
      At La Chinacata we strive to create unforgettable culinary experiences for
      each event. But don't just take our word for it, listen to what our
      satisfied customers say!
    </p>
  </header>
  <div class:list={["mx-auto", "max-w-screen-sm", "pt-10"]}>
    {
      reviews.map((review) => (
        <div class:list={["flex flex-col sm:flex-row gap-6"]}>
          <Image
            src={review.image}
            alt={review.name}
            class:list={[
              "basis-1/2",
              "rounded-3xl",
              "mx-auto",
              "object-contain h-80",
            ]}
          />
          <div
            class:list={[
              "flex flex-col basis-1/2",
              "[&_p]:text-center [&_p]:text-lg",
              "font-medium text-pretty text-green-leaf-800",
            ]}
          >
            <p>
              <span class="align-sub font-serif font-bold text-4xl">"</span>
              {review.review}
              <span class="align-sub font-serif font-bold text-4xl">"</span>
            </p>
            <p class="italic pt-6">{review.name}</p>
          </div>
        </div>
      ))
    }
  </div>
</section>
